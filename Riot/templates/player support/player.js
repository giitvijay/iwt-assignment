const getCard = document.getElementsByClassName('card')[0];
const p1 = document.getElementsByClassName('card__player-1')[0];
getCard.style.height = p1.naturalHeight;
getCard.style.width = p1.naturalWidth;
